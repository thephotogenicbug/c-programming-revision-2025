#include <stdio.h>

int main()
{

    // syntax

    // test_condition ? expression1 : expression2;

    int age = 20;

    (age >= 18) ? printf("You can vote!") : printf("You are not eligible to vote!");

    return 0;
}